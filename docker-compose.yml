services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - app-data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - MECAB_DICDIR=/var/lib/mecab/dic/ipadic
volumes:
  app-data: